Siguiente bloque:
- Crear endpoint GET /api/trips/{tripId}/summary
    - Calcular total gastado en el viaje (sumar expenses.amount)
    - Calcular total pagado por cada participant
    - Devolver estructura: trip + participants + balances (v1 simple)
